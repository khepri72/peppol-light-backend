🚀 CORRECTION DÉFINITIVE + REDÉPLOIEMENT - Erreur Mobile Overlay Rouge

═══════════════════════════════════════════════════════════════════
🎯 OBJECTIF FINAL
═══════════════════════════════════════════════════════════════════

- ✅ Corriger l'erreur runtime à la source (ErrorBoundary + i18n)
- ✅ Désactiver l'overlay Vite HMR (ne plus jamais afficher le rouge)
- ✅ Rebuild propre + redéploiement
- ✅ Chargement direct sur mobile sans erreur visible

═══════════════════════════════════════════════════════════════════
ÉTAPE 1 : VÉRIFICATION & CORRECTION i18n
═══════════════════════════════════════════════════════════════════

📁 FICHIER : client/src/i18n/config.ts

VÉRIFIER que cette configuration existe :
```typescript
// Dans la config i18next
react: {
  useSuspense: false  // ← CRITIQUE !
}
```

SI ABSENT, ajouter dans la config i18next existante.
Cette option empêche les erreurs de Suspense React.

═══════════════════════════════════════════════════════════════════
ÉTAPE 2 : VÉRIFICATION ErrorBoundary
═══════════════════════════════════════════════════════════════════

📁 FICHIER : client/src/components/ErrorBoundary.tsx

VÉRIFIER que ce fichier existe avec un ErrorBoundary React standard.

SI ABSENT, créer ce fichier :
```typescript
import React, { Component, ReactNode } from 'react';

interface Props {
  children: ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: any) {
    console.error('🚨 ErrorBoundary caught:', error, errorInfo);
  }

  handleReload = () => {
    window.location.reload();
  };

  render() {
    if (this.state.hasError) {
      return (
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          minHeight: '100vh',
          padding: '20px',
          textAlign: 'center',
          backgroundColor: '#f5f5f5'
        }}>
          <h2 style={{ color: '#d32f2f', marginBottom: '16px' }}>
            Une erreur est survenue
          </h2>
          <p style={{ color: '#666', marginBottom: '24px' }}>
            {this.state.error?.message || 'Erreur inattendue'}
          </p>
          <button
            onClick={this.handleReload}
            style={{
              padding: '12px 24px',
              backgroundColor: '#1E5AA8',
              color: 'white',
              border: 'none',
              borderRadius: '8px',
              cursor: 'pointer',
              fontSize: '16px'
            }}
          >
            Recharger la page
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}
```

═══════════════════════════════════════════════════════════════════
ÉTAPE 3 : WRAPPER App AVEC ErrorBoundary
═══════════════════════════════════════════════════════════════════

📁 FICHIER : client/src/App.tsx

VÉRIFIER que l'app est wrappée :
```typescript
import { ErrorBoundary } from './components/ErrorBoundary';

function App() {
  return (
    <ErrorBoundary>
      {/* Tout le contenu existant de l'app */}
    </ErrorBoundary>
  );
}
```

SI PAS WRAPPÉ, ajouter le wrapper ErrorBoundary autour du contenu existant.

═══════════════════════════════════════════════════════════════════
ÉTAPE 4 : DÉSACTIVER L'OVERLAY VITE (SOLUTION PRAGMATIQUE)
═══════════════════════════════════════════════════════════════════

📁 FICHIER : vite.config.ts

AJOUTER/MODIFIER la section server :
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    host: '0.0.0.0',
    port: 5000,
    strictPort: true,
    hmr: {
      overlay: false,  // ← DÉSACTIVE l'overlay rouge
      clientPort: 443,
      protocol: 'wss'
    }
  },
  // ... reste de la config existante
});
```

IMPORTANT : Conserver toute la config existante, ajouter seulement `overlay: false`.

═══════════════════════════════════════════════════════════════════
ÉTAPE 5 : AJOUTER LOGS DE VERSION (DEBUG)
═══════════════════════════════════════════════════════════════════

📁 FICHIER : client/src/main.tsx

AJOUTER au tout début du fichier (avant les imports si possible, sinon juste après) :
```typescript
console.log('🚀 Peppol Light v2.1 - ErrorBoundary + HMR overlay OFF');
console.log('📅 Build:', new Date().toISOString());
console.log('🔧 Config: useSuspense=false, overlay=false');
```

Ces logs permettent de vérifier quelle version est chargée.

═══════════════════════════════════════════════════════════════════
ÉTAPE 6 : NETTOYAGE CACHE + REBUILD
═══════════════════════════════════════════════════════════════════

EXÉCUTER dans le terminal :
```bash
# 1. Nettoyer le cache Vite et dist
rm -rf node_modules/.vite
rm -rf dist

# 2. Rebuild complet
npm run build

# 3. Redémarrer le dev server
npm run dev
```

VÉRIFIER qu'il n'y a pas d'erreur de build dans les logs.

═══════════════════════════════════════════════════════════════════
ÉTAPE 7 : REDÉPLOIEMENT
═══════════════════════════════════════════════════════════════════

1. **Cliquer sur le bouton "Republish"** dans l'interface Replit
2. **Attendre** la fin du déploiement (observer les logs)
3. **Vérifier** qu'aucune erreur n'apparaît dans les logs de déploiement

═══════════════════════════════════════════════════════════════════
ÉTAPE 8 : TESTS DE VALIDATION
═══════════════════════════════════════════════════════════════════

🖥️ TEST DESKTOP :

1. Ouvrir la console du navigateur
2. Recharger la page
3. Vérifier les logs :